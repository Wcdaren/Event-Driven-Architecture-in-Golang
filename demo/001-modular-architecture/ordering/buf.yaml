version: v2
breaking:
  use:
    - FILE
lint:
  enum_zero_value_suffix: _UNSPECIFIED  # Changed from _UNKNOWN to _UNSPECIFIED
  ignore:
    - PACKAGE_VERSION_SUFFIX
    - PACKAGE_DIRECTORY_MATCH          # Changed from except to ignore